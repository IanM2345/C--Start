#include <iostream>
#include <cmath>

int main (){
   double x = 6;
   double y = 3;
    
   //double z = std::max(x,y);
   //double z = std::min(x,y);
  // double z = pow(x,y);
   //double z = sqrt(9);
   //double z = abs(6);
   // double z = round(x);
   // double z = ceil(x);
   double z = floor(x);

    std:: cout << z <<'\n';

    return 0;
}